// /** @type {import('next').NextConfig} */
// const nextConfig = {};


/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.pexels.com", "images.unsplash.com"], // allow Pexels + Unsplash
    },
};

export default nextConfig;
// module.exports = nextConfig;
